# 4.2. Práctica — Comparación GUI vs CLI en Ubuntu e instalación de KDE
Abre tu máquina virtual de Ubuntu. Los objetivos de estas práctica son: Comprender las diferencias entre trabajar con GUI y CLI en Ubuntu, realizar tareas equivalentes en ambos entornos e instalar y probar un nuevo desktop environment (KDE Plasma) mediante CLI. Haz capturas del proceso y entrega un archivo pdf.

## 1- Ejecuta el siguiente código en bash

```bash
uname -a
lsb_release -a
```
- A partir de este comando, saca la versión del kernel y la versión del S.O.

Ahora, en vez de usar el comando, vas a intentar buscar esta información en el equipo. Para ello, busca en información del sistema  la información proporcionada por el comando. Es decir, algo así:

![Texto alternativo](/home/manolo/Imágenes/captura_practica.png)

## 2- Ejecuta el siguiente código en bash

```bash
ps -eo comm,%cpu,%mem --sort=-%mem | head -15
```

La salida del comando debe ser algo parecido a lo siguiente:

![](/home/manolo/Imágenes/captura_practica2.png)

Busca en monitorización del sistema la información proporcionada por el comando y comprueba, ordenando por memoria, que las salidas coinciden.

¿Por qué Firefox tiene distintos procesos corriendo al mismo tiempo? Qué ventajas tiene dividir Firefox en múltiples procesos e hilos en lugar de un único proceso?

## 3-Instala la GUI de KDE y pruébala al cerrar sesión

```bash
sudo apt install kde-plasma-desktop -y
```

Cierra sesión, dale a la ruedita de configuración y comprueba que puedes cambiar la interfaz de usuario.

## 4-Responde coherentemente a las siguientes preguntas a partir del archivo de Teoría del punto 4 o información que encuentres en Internet

1-¿Qué diferencia hay entre un programa y un proceso?

2-¿Qué recursos asigna el sistema operativo a un proceso cuando lo crea?

3-¿Qué es un hilo y qué partes del contexto tiene propias y cuáles comparte con su proceso padre?

4-¿En qué se diferencia un núcleo físico de un núcleo lógico (HyperThreading/SMT)?

5-¿Qué es el PID, TID y PID de un proceso?

6-¿Qué ventajas tiene dividir Firefox en múltiples procesos e hilos en lugar de un único proceso? Contesta.

Ahora, escribe en el navegador de firefox:`about:processes`. Fijate en un proceso que actúe sobre una ventana y quédate con el PID de ese proceso (el numerito que aparece al lado del mismo). 

Posteriormente, a partir de ese número (llamado PID), ejecuta;

`ps -L -p <PID> -o pid,tid,psr,comm` 

Busca información sobre lo anterior. ¿Cuantos hilos corre este proceso? Prueba a matar el proceso que maneja una ventana con:

`kill <PID>`

¿Qué es lo que sucede?

7-Si tu procesador tiene 4 núcleos físicos con HyperThreading, ¿cuántos procesadores lógicos detectará el sistema operativo o el propio virtualizador?

8-¿Qué estados puede tener un proceso en Linux (ejemplo: R, S, Z, T)?